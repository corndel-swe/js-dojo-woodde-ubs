/**
 * Calculates the adjusted salary after applying a raise and a tax deduction.
 * @param {number} salary - The initial salary.
 * @param {number} raiseRate - The raise rate as a percentage.
 * @param {number} taxRate - The tax rate as a percentage.
 * @returns {number} The adjusted salary.
 *
 * Example: adjustedSalary(50000, 10, 20) should return 44000.
 */
export function adjustedSalary(salary, raiseRate, taxRate) {
  // TODO: Calculate the salary after the raise
  // TODO: Calculate the salary after the tax deduction
  // TODO: Return the adjusted salary
}
